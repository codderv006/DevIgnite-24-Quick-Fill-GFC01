import 'package:flutter/material.dart';

class SlotBookingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Slot Booking'),
      ),
      body: Center(
        child: Text('Slot Booking Page'),
      ),
    );
  }
}
